import React from 'react';
import '../App/app.css';

function FilterButton({  }) {
  return (
    <button onClick={() => handleClick()}>
      Add {}
    </button>
  );
}

export default FilterButton;
